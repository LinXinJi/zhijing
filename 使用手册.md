# 知径 · 完整操作手册

> 从安装到毕业，读这一份就够。设计细节见 `docs/`，调参与疑难见 `docs/12`。
> 系统名："知径"—— 一个住在终端里的教学 Agent。**日常你只需要说两句话。**

---

## 1. 系统是什么（30 秒）

- **Pi agent = 手**（读写文件、执行命令、权限与审计）；**DeepSeek V4 Pro = 脑**（讲解、规划）；
  工作区根目录的 `AGENTS.md`（`你的学习工作区\AGENTS.md`）+ `skills\` + 各目标的 `graph.json` = 它的**操作手册与记忆**。
- 它会：**探寻**你已知什么 → 算出"**权重最小**"的学习路径 → 计划确认后**出学习规划图** → **演示** → 一课一课**教学**（带来源、连旧知、多视角、练习巩固）→ 反馈校准 → 学完**导出完整知识图谱**并归档。
- 两条防线：**五级事实分级 + 来源**（防它幻觉）；**生成式回忆 + 门禁 + 校准**（防我自欺）。
- **你只负责说**："我想学习 X" 和 "继续学习"（学完时加一句"我学完了"）。

## 2. 安装与配置（一次性，约 10 分钟）

1. 安装 Pi agent：[github.com/badlogic/pi-mono](https://github.com/badlogic/pi-mono)（npm 全局安装）；
2. 接入 DeepSeek：官方指引 [api-docs.deepseek.com · Pi 集成](https://api-docs.deepseek.com/quick_start/agent_integrations/pi_mono/)——`DEEPSEEK_API_KEY` + 模型 `deepseek-chat`（日常教学）/ `deepseek-reasoner`（规划难题）；
3. 验证：任意目录打开 pi 说"你好"，能正常对话即可。

> **两种运行方式**
> - **方式 A · 开箱即用**：`python zhijing.py` —— 本地终端教学 Agent，无需 Pi 与 API；
>   设置环境变量 `DEEPSEEK_API_KEY` 后自动启用 LLM 讲解/提问/解答（可选）。
> - **方式 B · 完整方案**：Pi agent + DeepSeek V4 Pro（协议驱动），按本手册其余章节操作。

## 3. 目录与文件（三区：谁放哪里）

```
你的学习工作区\
├─ AGENTS.md             ← 知径协议（根目录，任意子目录开会话自动生效）
├─ 背景知识.md            ← 总档案：AI 探寻出的我的背景知识（每次探寻后追加）
├─ 读书札记\              ← 读书时的疑问与感悟收件箱（"记录个疑问/有个感悟"捕获）
├─ 学习记录\
│   ├─ 正在学习\          ← 进行中的目标（"继续学习"在这里找）
│   │   └─ <目标>\        ← graph.json / 会话记录.md(摘要) / 课堂记录\(逐字) / 图谱导出\(四件套) / 素材\ / 复习\<知识点>\ / 三本账
│   └─ 已经学完\          ← 毕业的目标（学完自动归档；参考会话也在这里）
└─ 本仓库（系统本体）        ← 只读，不写学习数据；移植 = 复制整个仓库 + AGENTS.md
    ├─ docs\ skills\ tools\ templates\ prototype\ tests\ pi\ 使用手册.md
```

| 系统文件 | 作用 |
|---|---|
| `skills/zhijing/SKILL.md` | 教学技能卡：命令实现规范、数值规则、出图规则 |
| `skills/zhijing/探寻问卷.md` | 探寻阶段的宽→中→细标准问法 |
| `tools/graph_ops.py` | 确定性计算内核（13 个子命令）：路径/m/间隔/校准/导出/归档，**LLM 只讲解不口算** |
| `templates\` | **文档模板包**（文档规范.md + 学习报告/会话记录/课堂记录/复习记录/学习时间线/分阶段计划/读书札记 8 套模板）+ graph.模板.json |
| `速查卡.md` | 功能与触发方式速查（一句话触发清单） |
| `tests/smoke_test.py` | 工具回归测试（改代码后跑一遍） |
| `docs/01–12` | 完整设计文档 |

## 4. 首次使用（约 30–45 分钟）

0. **方式 A**：终端运行 `python zhijing.py`，说"我想学习 <主题>"即可——探寻、教学、复习、札记全程交互（`/help` 看命令）；**方式 B** 继续下面步骤：
1. 在 `你的学习工作区`（或任意子目录）打开 pi；
2. 说：**"我想学习 <主题>"**（如 `我想学习 Python 装饰器`）——它自动建 `学习记录\正在学习\<主题>\` 并开始**探寻**；
3. 跟着探寻走（宽→中→细；别抢答；说"不会"零成本）；结束它会写回图谱 + 把背景摘要追加到根目录 `背景知识.md`——**核对**："我的已知区是这些，对吗？"；
4. 它给**阶段化网状规划**（主线 + 可并行支线）→ 你确认计划 → 它用 diagram-design 生成**学习规划图**；计划是活文档，学习中会随掌握度/关联度动态重排（`图谱导出\规划图.svg`）→ `/demo` 演示 → 认可后开始上课。

## 5. 日常一次课（标准流程）

说 **"继续学习"**，它自己找到正在学的目标接着走。**七步课**：激活锚点（回忆旧知）→ 讲解新点（只用已知作锚，带来源）→ 显式连接（建边，并主动 `find-links` 找隐性联系）→ 多视角 → 兴趣扩展 → 练习（`/quiz`）→ 反思+收口（`/feel`）。

示例对话（简）：

```
我: 继续学习
pi: [已找到 正在学习\装饰器] 目标=装饰器；第 3 课: 高阶函数
pi: [激活锚点] 不看书，用自己的话讲"函数对象"？
我: 函数名可以赋值、可以放进列表、当参数传
pi: 对。高阶函数 = 把函数对象当参数或返回值（map/filter/sorted(key=...)）
    [L1]《流畅的 Python》§5.3 —— 你笔记里的 sorted(key=len) 就是它
pi: /quiz 高阶函数：先自评信心(0-100)，再答
我: 信心 85。接受函数作参数或返回函数的函数
pi: [记录] m 0.55→0.66，3 天后复习。今天哪里最卡？/feel
我: /feel 例子都懂了，负荷正好
```

**课后必做**：`/quiz`（成绩决定进度 m）与 `/feel`（感受决定讲法 r）——跳过它们，系统就失去了校准数据。

## 6. 命令速查表（协议命令，一般不用你手打）

| 命令 | 作用 | 命令 | 作用 |
|---|---|---|---|
| `/plan` | 推荐主线+备选对比（确认后出**规划图**） | `/roadmap` | 阶段化网状规划（主线+支线+阶段） |
| `/find-links` | 连接建议（不同节点间） | 说"找联系" | 同上 |
| `/demo` | 展示规划图与完整方案 | 说"看计划" | 阶段化网状规划 |
| `/teach` | 下一课（受门禁约束） | `/quiz <点>` | 生成式回忆测验（信心→作答→自评） |
| `/review` | 到期复习清单 | `/review-node <点>` | 复习卡（"我要复习 X"用） |
| `/calibrate` | 信心 vs 实际面板 | `/source <点>` | 出示定义/来源/级别 |
| `/doubt` | 质疑→复查→纠错日志 | `/import <路径>` | 导入笔记/材料（抽点→确认→入图） |
| `/undo` | 回滚最近一次事件 | `/graph` | 看图谱+导出 Mermaid |
| `/graduate` | 终局图谱+过程报告 | `/finish <目标>` | 学完归档到 已经学完\ |
| `/progress` | 单目标进度面板 | 问"学到哪了" | 全部并行线总览（progress-all） |
| `/assess <点>` | 分数+多标签评价 | `/assess-all` | 全部知识点评价总览 |
| `/commit` | 像 git 提交学习记录 | `/log` | 回望提交历史 |
| 说"记录个疑问/有个感悟" | 捕获到读书札记 | `/captures` | 札记列表 |
| `/status` | 图谱状态+进度 | `/help` | 全部命令 |

对应确定性内核：`python tools\graph_ops.py <子命令> --graph 学习记录\正在学习\<目标>\graph.json`。**pi 负责把你说的话翻译成这些调用，并负责讲解与对话。**

## 7. 反馈与校准（系统的活水）

- 每次课末 `/feel` 三问：哪里最卡？哪个类比没懂？负荷如何？
- 双周 `/calibrate`：信心 vs 实际曲线，"以为会 vs 真会"差距榜；
- **分工**：成绩 → m（学到哪）；感受 → r 与讲法（怎么教）。感受永远不能单独决定"学会没有"；
- 误操作随时 `/undo`；被质疑时 pi 会复查来源、认错并写纠错日志；
- **评价体系**：每个知识点有**分数（0–100 = m×50 + 复习×15 + 表现×20 + 校准×15）**与**多标签**（难度/掌握/熟练/稳定/关键前提/卡点/兴趣）；每次复习后自动评估，写回图谱与复习记录；权值系数 (1+λ(1−m)) 随熟悉度下降，复习记录里可见前后对比。

## 8. 材料接入

`/import 路径`（你的笔记、`系统学习\` 下的资料）→ 抽候选点 → **逐条向你确认** → 入图，来源锚定"文件+位置"。你的笔记是最高信任来源，讲解优先锚定它。

## 8.5 读书场景：看不懂 & 有感悟

- **看不懂**：说"记录个疑问：<内容>" → 落 `读书札记\`（时间戳 + 出处 + 原文 + 困惑）→ AI **立即**用已知作锚解答（L1–L5 + 来源）→ 若发现缺前置知识 → 提议用 `add-node` / `add-edge` 补进当前学习路径或开新目标（状态：待补课）；
- **有感悟**：说"有个感悟：<内容>" → 落札记 → AI 提炼"这像不像 X 与 Y 的关系？" → 你确认后**用 `add-edge` 建高关联边**（来源=我的感悟；新概念先 `add-node`）→ 出现在知识路径图 / 毕业图谱的"我的连接"（状态：已入图谱）；
- **回看**："看看我的札记" → 列表（待处理 / 已解答 / 待补课 / 已入图谱）；"处理札记" → 逐条过。

## 9. 出图与复习记录（自动生成）

| 产出 | 位置 | 时机 |
|---|---|---|
| **计划的图** | `图谱导出\规划图.svg` | 计划经你确认后（diagram-design，数据 = plan 输出） |
| **知识路径的图** | `图谱导出\知识路径图.svg` | 每个里程碑与毕业时刷新（实线=教过·第几课；虚线=未走/换锚点/你的类比；颜色=掌握度分区） |
| **学习报告** | `图谱导出\学习报告.md` | `/graduate`（**含成绩单表格**：分数/m/难度/掌握/熟练/稳定/卡点/复习次数，按分数排序 + 概况/时间线/每点掌握史/卡点/校准/产出清单） |
| **详细课堂对话记录** | `课堂记录\第N课-<点>.md` | **上课时由"课堂记录子 agent"实时逐字落盘**（七步问答/测验/感受/提示级别；模板见 skills\zhijing\课堂记录模板.md）；`会话记录.md` 保留摘要 |
| **复习记录** | `复习\<知识点>\YYYY-MM-DD-第N次.md` | 每次复习一条，**自动生成成绩单**（复习前状态/三轮问答逐字/提示级别/成绩/分数与标签/复习后 m 与下次时间/权值系数变化/暴露问题） |
| **学习时间线** | `图谱导出\学习时间线.md` + 提交历史 | 每个事件带时间戳（探寻/教学/测验/感受/导入/提交/毕业）；每课与每次复习结束 **`commit` 一笔**（像 git）；`log` 随时回望；毕业自动画**时间线图**（节点=提交） |

- **毕业**：说"我学完了" → `/graduate` 导出上述报告与终局图谱（`graduate.mmd/.json`，**自动附 diagram-design 精美版**）→ `/finish` 归档到 `已经学完\`；
- **复习**：说"我要复习 X" → 复习卡三轮流程（无提示回忆 → 变式/反例 → 连接抽问；答不上才逐级提示）→ 记分更新 m 与间隔 → 记录落盘 `复习\X\`；
- 概念讲解时自动配示意图；**图型由 AI 自主选择**（路径→架构图/有向图、流程→流程图、概念→思维导图、进度→仪表盘风，不强制 Mermaid，优先美观清晰整洁）；颜色语义固定：绿=已掌握、黄=半知、灰=未知；**图上数据与文字必须和状态文件一致**（图也有幻觉）；
- **产出不阻塞学习**：规划图、学习报告、终局图谱、时间线图等产出任务由**子 agent（后台）**并行生成，主流程继续教学/复习，完成后回报文件路径；子 agent 只读状态、只写 `图谱导出\`、不改 graph.json。

## 10. 常见问题（速查；详见 docs/12）

| 现象 | 处理 |
|---|---|
| 会话断了/上下文满了 | 状态在文件里：直接说"继续学习" |
| 下课直接退出了 | 没关系：每次行动已即时落盘，下次"继续学习"自动检测未收尾并补记 |
| 背景知识没更新 | 启动/退出自动同步（`sync-background`）："当前学习目标/已学完"节始终与实际学习记录一致 |
| 多个目标同时在学 | 说"继续学习"它会问学哪个；问"学到哪了"看全部并行线进度 |
| 记不清自己的进度 | 问"学习进度怎么样"→ 全部总览；问"X 学到哪了"→ 该目标的科学面板（综合%=0.6×目标m+0.4×路径完成度） |
| 路径感觉不对 | `/feel --r A B 新r` 或 `/doubt` → `/plan` 看变化（规划图随之重出） |
| 门禁一直卡 | 真没会就继续 `/quiz`；题偏了让它换变式题 |
| 想复习某个点 | 说"我要复习 X"→ 三轮流程（回忆→变式→连接），答不上才提示；记录落盘 `复习\X\` |
| 误打分 | `/undo` |
| 图不连通 | 回探寻补桥梁点，或 `/import` |
| 参数怎么调 | `docs/12` §1 参数表；一次只动一个 |

## 11. 对照与自检

- 行为标准：`学习记录\已经学完\` 下的 导数-参考会话 与 装饰器-参考会话；
- 回归自检：改过 `tools/graph_ops.py` 后跑 `python tests\smoke_test.py`。

---

## 附录：首次会话开场白（复制粘贴）

```
读 AGENTS.md 与 skills/zhijing/SKILL.md，按知径协议执行。
我想学习：<主题>。
注意：路径/代价/掌握度/间隔一律用 tools/graph_ops.py 计算，不要口算；
每条事实标 L1–L5 并给来源；不确定就说不知道；每个行动写 audit.log（目标目录内）；
每课结束把过程摘要追加到该目录的 会话记录.md；探寻结束把背景摘要追加到根目录 背景知识.md。
```

之后的每次会话，一句话即可：**"继续学习"**；学完了就说：**"我学完了"**。
